#include "main.h"
/**
 * add - A program that computes addittion
 * @a: the number to be computed
 * @b: the number to be computed
 * Return: 0 (Success)
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (0);
}
