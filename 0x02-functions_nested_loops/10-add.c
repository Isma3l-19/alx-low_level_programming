#include "main.h"
/**
 * add - A program that computes addittion
 * @a: the number to be computed b
 * @b: the number to be computed a
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int sum;

	sum = a + b;

	return (sum);
}
