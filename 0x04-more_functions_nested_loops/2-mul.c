#include "main.h"
/**
 * mul - A program that multiplies two integers
 * @a: the character to multiplied to b
 * @b: the character to be multiplied to a
 * Return: the multiplicated
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
